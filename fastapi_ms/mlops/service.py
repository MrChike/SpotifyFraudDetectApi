from .inference import * #inference is the only operation that should be exposed via api others are internal operations

def service_calculation():
    updated_formula = 100 + 100
    return updated_formula
